services:
  proomet-hub:
    build: .
    image: proomet-hub:latest
    container_name: proomet-hub
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATA_DIR=/data
      # - GITHUB_TOKEN=  # 可通过设置页配置，或此处注入
    volumes:
      - ./data:/data
